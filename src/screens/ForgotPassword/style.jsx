import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    padding: 10
  },

  header: {
    flexDirection: "row",
    alignItems: "center",
    columnGap: 20,
    marginVertical: 20
  },

  heading: {
    fontSize: 24,
    color: "#000",
    fontWeight: "bold",
    
  }
})

export default styles;